# Flask User Counter App

A simple Flask + Peewee + JWT app where each user has a personal counter.

## Run locally

pip install -r requirements.txt
python server.py

## Deploy

Use Render, Railway, or GitHub Codespaces.